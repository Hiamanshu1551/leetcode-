class Solution {
    public boolean isPerfectSquare(int num) {
        int a = (int) Math.sqrt(num);
        if (num % (a * a) == 0) {
            return true;
        }
        return false;
    }
}
