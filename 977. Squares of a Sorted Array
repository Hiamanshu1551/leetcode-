class Solution {
    public int[] sortedSquares(int[] nums) {
        int[] arr=new int[nums.length];
        int n=0;
        for(int i=0;i<nums.length;i++){
            arr[n++]=nums[i]*nums[i];
        }

      Arrays.sort(arr);
      return arr;
    }
}
